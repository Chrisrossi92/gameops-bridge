PORT=3001
API_HOST=0.0.0.0
NODE_ENV=development
# Production default allows only https://servers.cdawgbot.xyz.
# Override with a comma-separated list only when intentionally exposing another dashboard origin.
API_CORS_ORIGIN=https://servers.cdawgbot.xyz
KNOWN_PLAYER_STORE_PATH=../known-players.json
SESSION_STATE_STORE_PATH=../session-state.json
